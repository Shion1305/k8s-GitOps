apiVersion: v1
kind: Service
metadata:
  name: provider-monitor
  namespace: provider-monitor
  labels:
    app: provider-monitor
spec:
  type: ClusterIP
  ports:
    - port: 9877
      targetPort: 9877
      protocol: TCP
      name: metrics
  selector:
    app: provider-monitor
