apiVersion: longhorn.io/v1beta2
kind: BackupTarget
metadata:
  name: default
  namespace: longhorn-system
spec:
  # Empty backup target URL disables actual backup functionality
  # but satisfies the validation requirement
  backupTargetURL: ""
  credentialSecret: ""
  pollInterval: "300s"
