defaultSettings:
  # Use v1 data engine (traditional filesystem-based)
  v2DataEngine: false
  defaultReplicaCount: 1
  # Do not create default disks
  createDefaultDiskLabeledNodes: false
  # Disable default data path since we use specific disk configurations
  defaultDataPath: ""
  # Reservation rate for filesystem-based volumes
  storageReservedPercentageForDefaultDisk: 25
  # Allow Longhorn to run on control-plane nodes
  taintToleration: "node-role.kubernetes.io/control-plane:NoSchedule"
# Disable automatic StorageClass creation by Longhorn
# We manage StorageClasses via GitOps in storageclass.yaml
persistence:
  defaultClass: false
  defaultClassReplicaCount: 1
# Allow Longhorn manager to run on all storage nodes including control-plane
longhornManager:
  tolerations:
    - key: node-role.kubernetes.io/control-plane
      operator: Exists
      effect: NoSchedule
# Allow Longhorn driver (CSI plugin) to run on all storage nodes including control-plane
longhornDriver:
  tolerations:
    - key: node-role.kubernetes.io/control-plane
      operator: Exists
      effect: NoSchedule
# Engine image deployment - allow on control-plane
engineImage:
  tolerations:
    - key: node-role.kubernetes.io/control-plane
      operator: Exists
      effect: NoSchedule
# UI deployment
longhornUI:
  nodeSelector:
    kubernetes.io/hostname: shion-ubuntu-2505
preUpgradeChecker:
  jobEnabled: false
