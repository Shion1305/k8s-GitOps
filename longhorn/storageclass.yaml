apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: longhorn-ssd
  annotations:
    storageclass.kubernetes.io/is-default-class: "true"
provisioner: driver.longhorn.io
allowVolumeExpansion: true
reclaimPolicy: Retain
parameters:
  numberOfReplicas: "1"
  dataEngine: "v1"
  fsType: "ext4"
  diskSelector: "ssd" # This must match with Node CR disk.tags
volumeBindingMode: Immediate
---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: longhorn-hdd
provisioner: driver.longhorn.io
allowVolumeExpansion: true
reclaimPolicy: Retain
parameters:
  numberOfReplicas: "1"
  dataEngine: "v1"
  fsType: "ext4"
  diskSelector: "hdd"
volumeBindingMode: Immediate
---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: longhorn-hdd-ha
  annotations:
    storageclass.kubernetes.io/description: "High availability HDD storage with 2 replicas for critical workloads (uses all HDD nodes)"
provisioner: driver.longhorn.io
allowVolumeExpansion: true
reclaimPolicy: Retain
parameters:
  numberOfReplicas: "2"
  dataEngine: "v1"
  fsType: "ext4"
  diskSelector: "hdd-ha"
  # Prefer spreading replicas across different nodes
  replicaAutoBalance: "best-effort"
volumeBindingMode: Immediate
