apiVersion: v1
kind: ServiceAccount
metadata:
  name: eso
  namespace: lumos-bot
---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: vault
  namespace: lumos-bot
spec:
  provider:
    vault:
      server: "http://vault.vault.svc.cluster.local:8200"
      path: "lumos-bot"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "eso-lumos-bot"
          serviceAccountRef:
            name: "eso"
