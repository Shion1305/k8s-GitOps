global:
  domain: argocd.k.shion1305.com
configs:
  # Main configuration
  cm:
    # Admin account
    accounts.admin.enabled: "true"
    # Anonymous access - allow unauthenticated users to access ArgoCD
    users.anonymous.enabled: "true"
    # Application URL
    url: https://argocd.k.shion1305.com
    # Dex (GitHub OAuth) configuration
    dex.config: |
      connectors:
      - type: github
        id: github
        name: GitHub
        config:
          clientID: $github-oauth-secret:dex.github.clientID
          clientSecret: $github-oauth-secret:dex.github.clientSecret
          redirectURI: https://argocd.k.shion1305.com/api/dex/callback
          # Load GitHub organization and teams for RBAC mapping
          # This allows ArgoCD to map team memberships to roles
          orgs:
            - name: Shion1305Dev
              # Teams within this org that can access ArgoCD
              # If teams list is empty or omitted, all org members can authenticate
              # Teams are mapped in RBAC policy.csv below
          teamNameField: slug
          useLoginAsID: false
    # Resource exclusions
    resource.exclusions: |
      ### Network resources created by the Kubernetes control plane and excluded to reduce the number of watched events and UI clutter
      - apiGroups:
        - ''
        - discovery.k8s.io
        kinds:
        - Endpoints
        - EndpointSlice
      ### Internal Kubernetes resources excluded reduce the number of watched events
      - apiGroups:
        - coordination.k8s.io
        kinds:
        - Lease
      ### Internal Kubernetes Authz/Authn resources excluded reduce the number of watched events
      - apiGroups:
        - authentication.k8s.io
        - authorization.k8s.io
        kinds:
        - SelfSubjectReview
        - TokenReview
        - LocalSubjectAccessReview
        - SelfSubjectAccessReview
        - SelfSubjectRulesReview
        - SubjectAccessReview
      ### Intermediate Certificate Request excluded reduce the number of watched events
      - apiGroups:
        - certificates.k8s.io
        kinds:
        - CertificateSigningRequest
      - apiGroups:
        - cert-manager.io
        kinds:
        - CertificateRequest
      ### Cilium internal resources excluded reduce the number of watched events and UI Clutter
      - apiGroups:
        - cilium.io
        kinds:
        - CiliumIdentity
        - CiliumEndpoint
        - CiliumEndpointSlice
      ### Kyverno intermediate and reporting resources excluded reduce the number of watched events and improve performance
      - apiGroups:
        - kyverno.io
        - reports.kyverno.io
        - wgpolicyk8s.io
        kinds:
        - PolicyReport
        - ClusterPolicyReport
        - EphemeralReport
        - ClusterEphemeralReport
        - AdmissionReport
        - ClusterAdmissionReport
        - BackgroundScanReport
        - ClusterBackgroundScanReport
        - UpdateRequest
    # Resource update ignore rules
    resource.customizations.ignoreResourceUpdates.ConfigMap: |
      jqPathExpressions:
        # Ignore the cluster-autoscaler status
        - '.metadata.annotations."cluster-autoscaler.kubernetes.io/last-updated"'
        # Ignore the annotation of the legacy Leases election
        - '.metadata.annotations."control-plane.alpha.kubernetes.io/leader"'
    resource.customizations.ignoreResourceUpdates.Endpoints: |
      jsonPointers:
        - /metadata
        - /subsets
    resource.customizations.ignoreResourceUpdates.all: |
      jsonPointers:
        - /status
    resource.customizations.ignoreResourceUpdates.apps_ReplicaSet: |
      jqPathExpressions:
        - '.metadata.annotations."deployment.kubernetes.io/desired-replicas"'
        - '.metadata.annotations."deployment.kubernetes.io/max-replicas"'
        - '.metadata.annotations."rollout.argoproj.io/desired-replicas"'
    resource.customizations.ignoreResourceUpdates.argoproj.io_Application: |
      jqPathExpressions:
        - '.metadata.annotations."notified.notifications.argoproj.io"'
        - '.metadata.annotations."argocd.argoproj.io/refresh"'
        - '.metadata.annotations."argocd.argoproj.io/hydrate"'
        - '.operation'
    resource.customizations.ignoreResourceUpdates.argoproj.io_Rollout: |
      jqPathExpressions:
        - '.metadata.annotations."notified.notifications.argoproj.io"'
    resource.customizations.ignoreResourceUpdates.autoscaling_HorizontalPodAutoscaler: |
      jqPathExpressions:
        - '.metadata.annotations."autoscaling.alpha.kubernetes.io/behavior"'
        - '.metadata.annotations."autoscaling.alpha.kubernetes.io/conditions"'
        - '.metadata.annotations."autoscaling.alpha.kubernetes.io/metrics"'
        - '.metadata.annotations."autoscaling.alpha.kubernetes.io/current-metrics"'
    resource.customizations.ignoreResourceUpdates.discovery.k8s.io_EndpointSlice: |
      jsonPointers:
        - /metadata
        - /endpoints
        - /ports
  # RBAC configuration
  rbac:
    # Default role for all users (both authenticated and anonymous)
    # Set to custom 'role:guestbook-viewer' which can ONLY view the guestbook app
    # Built-in 'role:readonly' has too many permissions (can see all apps)
    # Authenticated admin users/teams will be elevated via group mapping below
    policy.default: role:guestbook-viewer
    # RBAC policies:
    # - Custom guestbook-viewer role: Can only view the guestbook application
    # - GitHub team members: Get full admin access
    # - Specific GitHub users: Get full admin access
    # - Built-in admin user: Retains admin role
    policy.csv: |
      # Define custom role 'guestbook-viewer' with minimal permissions
      # This role can ONLY view the guestbook application (nothing else)
      p, role:guestbook-viewer, applications, get, default/guestbook, allow

      # Explicitly assign built-in admin user to admin role
      g, admin, role:admin

      # Grant admin access to specific GitHub user(s)
      # GitHub username: Shion1305
      g, Shion1305, role:admin

      # Grant admin access to GitHub team members
      # Organization: Shion1305Dev, Team: k-shion1305-com-admin
      # All members of this team will have full admin access
      g, Shion1305Dev:k-shion1305-com-admin, role:admin
  # Reference to existing secrets
  secret:
    createSecret: false # We'll keep existing argocd-secret
  # Parameters (empty as per current config)
  params: {}
# Server configuration
server:
  # Disable default ingress since we have a custom one
  ingress:
    enabled: false
  # Keep existing service
  service:
    type: ClusterIP
# Dex configuration
dex:
  enabled: true
# Redis
redis:
  enabled: true
# Application controller
controller:
  replicas: 1
# Repo server
repoServer:
  replicas: 1
# ApplicationSet controller
applicationSet:
  replicas: 1
# Notifications controller
notifications:
  enabled: true
