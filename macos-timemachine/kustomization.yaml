apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: macos-timemachine

resources:
- namespace.yaml
- static-credentials.yaml
- configmap.yaml
- pvc.yaml
- deployment.yaml
- service.yaml
- networkpolicy.yaml

images:
- name: dperson/samba
  newTag: latest

labels:
- pairs:
    app: timemachine
    component: timemachine
