apiVersion: v1
kind: ServiceAccount
metadata:
  name: eso
  namespace: zot
---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: vault
  namespace: zot
spec:
  provider:
    vault:
      server: "http://vault.vault.svc.cluster.local:8200"
      path: "zot"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "eso-zot"
          serviceAccountRef:
            name: "eso"
