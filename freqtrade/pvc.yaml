apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: freqtrade-data
  namespace: freqtrade
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
